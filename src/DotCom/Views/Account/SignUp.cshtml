@{
    Layout = "_AccountLayout";
    ViewData["Title"] = "OwnApt, LLC - Sign Up";
    ViewData["MetaDescription"] = "Sign up for OwnApt Owner Portal access and gain insight into all of your account and property information.";
}

@model OwnApt.DotCom.Model.Account.LockContextModel

<script>
    var redirectUrl = '@Model.CallbackUrl';
    var state= '@Model.State';
    var nonce= '@Model.Nonce';

    var lockOptions = {
        closable: false,
        allowLogin: false,
        auth: {
            redirectUrl: redirectUrl,
            responseType: 'code',
            params: {
                state: state,
                nonce: nonce,
                scope: 'openid email'
            }
        },
        theme: {
            logo: '@Url.Content("~/images/Logo/Logo_50x48.png")',
            primaryColor: '#f05f40'
        },
        languageDictionary: {
            title: "OwnApt, LLC"
        }
    };

    var lock = new Auth0Lock('@Model.ClientId', '@Model.Domain', lockOptions).show();
</script>